<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ds.glzxj.mapper.GlzxjMappper">
    <select id="getGlzxjCount" resultType="integer">
        select count(*) from t_glzxj
    </select>

    <select id="findAllGlzxj" resultType="com.ds.glzxj.pojo.Glzxj">
        select * from t_glzxj order by glz_id desc
        <if test="offset!=null and limit!=null">limit #{offset},#{limit}</if>
    </select>

    <delete id="delAllGlzxj">
        delete from t_glzxj where glz_id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <insert id="insertGlzxj" parameterType="com.ds.glzxj.pojo.Glzxj">
        INSERT INTO t_glzxj ( glz_name,glz_data,glz_count ,glz_tq)
        VALUE (#{glz_name},now(),#{glz_count},2)
    </insert>



    <select id="toUpdWeekPlan" resultType="com.ds.weekPlan.pojo.WeekPlan" parameterType="com.ds.weekPlan.pojo.WeekPlan">
        select * from t_weekplan where z_id = #{z_id}
    </select>

    <update id="updWeekPlan">
      UPDATE
        t_weekplan
      SET
        z_name = #{z_name},z_bh = #{z_bh},
        z_one = #{z_one},z_two = #{z_two},
        z_three=#{z_three},z_four=#{z_four},
        z_five=#{z_five},z_six=#{z_six},
        z_sunday=#{z_sunday},z_remarks=#{z_remarks},
        z_startdate=#{z_startdate},z_enddate=#{z_enddate}
      WHERE
        z_id = #{z_id}
    </update>

    <!--还原-->
    <update id="huanyuanGLZ" parameterType="string">
      UPDATE
        t_glzxj
      SET
        glz_tq = 2
      WHERE
        glz_id = #{ids}
    </update>

    <!--提取-->
    <update id="tiquGLZ" parameterType="string">
      UPDATE
        t_glzxj
      SET
        glz_tq = 1
      WHERE
        glz_id = #{ids}
    </update>

    <update id="updateFQ" parameterType="com.ds.glzxj.pojo.Glzxj">
      UPDATE
        t_glzxj
      SET
        glz_user = #{glz_user}
      WHERE
        glz_id = #{glz_id}
    </update>

</mapper>